# How to use the question .py

This project uses `uv` for setup.

To run question 1 (q1), simply execute the script; you should see the result.

For question 2 (q2), you need to pass two arguments. For example:

```
uv run q2.py ./knowledge "What is the color of an apple"
```

For question 3 (q3) its in the md file explaining the steps that
